# AlphaQuant × Vibe-Trading 集成工作流

> 2026-08-30 集成版。本文档是完整工作流的权威说明：模块职责、调用顺序、
> 输入输出依赖、以及"需 API key 的组件被 vibe-trading 免费模块替代"的对照表。

## 0. 一图流

```
┌─────────────────────────────────────────────────────────────────────┐
│  python3 main.py run_all --codes 512480.SH,513100.SH,588000.SH     │
└─────────────────────────────────────────────────────────────────────┘
   │
   ├─(1) fetch_data ──► VibeMarketLoader ──► 子进程 broker (cwd=$HOME)
   │                      │                    └─ vibe fetch_market_data
   │                      │                       (tencent 链, 免token, qfq)
   │                      ├─► data/<code>.csv
   │                      └─► upsert alphaquant.db.market_data
   │
   ├─(2) calc_factors ─► RSRSCalculator(向量化, N=18, M=600)
   │                      ├─► market_data.rsrs_beta/r2/zscore 回写
   │                      └─► upsert factor_data
   │
   ├─(3) gen_signals ──► SignalGenerator.generate_rotation_signals
   │                      ├─► output/signals_YYYYMMDD.json (top_k 等权)
   │                      └─► (可选) RSRS+研报情感融合 [P3-03]
   │
   ├─(4) backtest ─────► VibeBacktestExporter
   │                      ├─► CSV 注入 vibe loader cache (tushare 槽位)
   │                      ├─► runs/<name>/config.json + code/signal_engine.py
   │                      └─► 子进程: python -m backtest.runner (cwd=$HOME)
   │                          ├─ 缓存全命中 → 不调 API、不需要 token
   │                          ├─ ChinaAEngine: T+1/整手/涨跌停/佣金
   │                          │   (ETF: stamp_tax=0)
   │                          └─► runs/<name>/ 回测结果
   │
   └─(5) monitor ──────► QDIICalculator (akshare) — 独立实时环节
```

## 1. 模块清单与职责

| 环节 | 模块 | 输入 | 输出 |
|---|---|---|---|
| 数据获取 | `src/data_engine/vibe_market_loader.py` + `scripts/vibe_fetch_broker.py` | codes, start, end | `data/<code>.csv`（date,open,high,low,close,volume）；`market_data` 表 |
| 因子计算 | `src/strategies/factors/rsrs.py` | `market_data` 行情 | `market_data.rsrs_*` 回写；`factor_data` 表 |
| 信号生成 | `src/strategies/signal_generator.py` | `factor_data`（+`report_sentiment` 可选） | `output/signals_*.json` |
| 策略回测 | `src/backtest/vibe_exporter.py`（+ vibe `backtest.runner`） | `data/*.csv`, 回测参数 | `runs/<name>/`（config、engine、结果） |
| 溢价监控 | `src/data_engine/qdii_calc.py` | 实时行情(akshare) | 日志告警 / Redis 频道 |
| 基本面分析（可选） | `src/llm_agent/analyzer.py` | 研报文本 | `report_sentiment` 表（缺省 0，不阻塞主链路） |

## 2. 调用顺序与依赖

```
fetch_data ──► calc_factors ──► gen_signals        # 生产链路（日频）
     └──────────────► backtest                      # 回测链路（复用同一份 CSV）
```

- `gen_signals` 依赖 `calc_factors` 的产出（factor_data 表）
- `backtest` 只依赖 `fetch_data` 的 CSV（信号由内联 signal_engine 重算，
  与 DB 无耦合，保证回测确定性）
- `monitor` 与链路无依赖，可常驻

## 3. 常用命令（WSL 内执行）

```bash
cd ~/workspace/alpha_quant

# 一步到位：拉数据 → 算因子 → 出信号 → 跑回测
python3 main.py run_all --codes 512480.SH,513100.SH,588000.SH \
    --start 2024-01-01 --start-fetch 2020-01-01 --top-k 2

# 分步执行
python3 main.py fetch_data   --codes 512480.SH,513100.SH --start 2020-01-01
python3 main.py calc_factors --codes 512480.SH,513100.SH -n 18 -m 600
python3 main.py gen_signals  --top-k 2 --fusion
python3 main.py backtest     --name my_run --codes 512480.SH,513100.SH,588000.SH \
                             --start 2024-01-01 --no-fetch   # 复用现有 csv
python3 main.py backtest     --name my_run2 --strategy rsrs_timing ...  # 换策略模板

# 滑动窗口回测（多策略 × 多窗口对比，覆盖不同市场周期）
python3 main.py sliding_backtest --name sliding_20260831 \
    --start 2023-01-01 --end 2026-06-30 \
    --window-months 6 --step-months 6 \
    --strategies rsrs_rotation,rsrs_timing,equal_weight
# 结果: runs/<name>/summary.md + summary.json + 每窗每策略一个子 run 目录

# QDII 溢价监控（独立，收盘快照）
python3 main.py monitor                      # 旧：实时轮询（while True）
python3 scripts/qdii_monitor.py              # 单次快照 -> runs/qdii_premium.md/json
python3 scripts/qdii_backtest.py             # 套利回测(读缓存) -> runs/qdii_backtest.md/json
python3 scripts/qdii_backtest.py --refresh   # 强制重拉历史溢价序列(每日追加最新数据点)
python3 scripts/qdii_daily.py                # 每日编排：监控快照 + 回测刷新(已接 crontab)
```

### 3.2 滑动窗口回测（sliding_backtest）

**动机**：单段回测（如 2024-01~2026-06 全牛市区间）无法区分策略 alpha 与市场 beta。
把评估期切成若干 6 个月窗口，逐窗跑同一批策略并横向对比，才能看出策略在
震荡/下跌/上涨不同周期里的稳定性。

**实现**（`src/backtest/sliding_window.py`）：

1. `build_windows` 按自然月切窗（默认 6 个月窗、6 个月步长，可重叠）。
2. **全局一次性拉数**：先按「首窗起点 − warmup_years(默认3年) ~ 末窗终点」
   确保三只标的的 csv 齐全（避免每个窗口各自调 broker 覆写同一 csv 反复重拉），
   已有 csv 首末日期覆盖需求（含 10 天容差，避开元旦等假日首日无 K 线）则跳过。
3. 逐窗口 × 逐策略实例化 `VibeBacktestExporter` 跑 vibe 回测，
   读取各自 `evaluation_metrics.json`（窗口口径指标）。
4. 聚合输出 `runs/<name>/summary.md`（三张表：各窗总收益 / 各窗超额 / 策略聚合）
   与 `summary.json`（机器可读全量），失败 run 记入 `failed_runs`。

**策略注册表**（`vibe_exporter.STRATEGY_TEMPLATES`，`--strategy` 选择）：

| 策略 | 逻辑 | 角色 |
|---|---|---|
| `rsrs_rotation` | 截面轮动：RSRS 修正分 top_k 等权，低于 min_score 空仓，日频调仓 | 主动策略 |
| `rsrs_rotation_weekly` | 同上，但每周首个交易日才调仓，权重持有整周 | 主动策略（低换手） |
| `rsrs_timing` | 单标的滞回择时：分 > +0.7 满仓、< −0.7 空仓，区间持有 | 主动策略（少换手） |
| `equal_weight` | 全部标的等权买入持有 | **被动基准的实盘化** |

`equal_weight` 有两个用途：一是作为「什么都不做」的对照组；二是校验基准口径——
`write_evaluation_metrics` 自算的 benchmark 就是窗口内等权买入持有，
所以 equal_weight 的超额应 ≈ 0（实测各窗 ±0.6% 以内，差异来自手续费），
若显著偏离说明基准或计费出了 bug。

新增策略 = 在 `STRATEGY_TEMPLATES` 里加一个信号引擎模板（vibe AST 沙箱限制：
只能用白名单语法与 pandas/numpy），无需改其他代码。

### 3.1 从 Windows 侧驱动 WSL（免切终端）

本项目的代码与依赖都在 WSL 里，但可以在 Windows 的 Git Bash 中直接驱动：

```bash
export MSYS_NO_PATHCONV=1            # 防止 Git Bash 把 /mnt/... 转成 C:/...
wsl.exe -- bash -c 'cd ~/workspace/alpha_quant && python3 main.py run_all --codes 512480.SH,513100.SH'
```

也可执行 Windows 盘上的脚本（WSL 把 C/D/I 盘挂在 `/mnt/c` 等）：

```bash
wsl.exe -- bash -c "/mnt/c/wb/task.sh"     # 退出码原样传回
```

**可靠性排序**（引号越多越容易踩坑）：

| 方式 | 适用场景 | 坑 |
|---|---|---|
| **写文件 → 执行文件**（推荐） | 多行 Python/复杂脚本 | 用 Write 工具写到 `\\wsl.localhost\...\home\hyz0906\xxx.py` 再 `python3 ~/xxx.py`，零转义问题 |
| `bash -c '...'`（单引号） | 短命令、含 `&&` | 内层双引号需写成 `\"`；**PowerShell 5.1 会吃掉引号，别用** |
| `bash -c "/mnt/c/x.sh"` | 现成脚本 | 路径用正斜杠；PowerShell 传参会吞反斜杠 |
| heredoc（`<<'EOF'`） | — | ❌ 经 wsl.exe 传递时转义会被破坏，实测报 `unexpected character after line continuation` |

其他注意点：

- 中文输出正常，退出码原样传递
- stderr 常出现 `wsl: Failed to translate '\Program Files\WorkBuddy\...'`，
  是 WorkBuddy 的 shim 目录触发的路径转换警告，**无害**，可用
  `2>&1 \| grep -v "Failed to translate"` 过滤
- 默认 cwd 不可依赖，命令里显式 `cd ~/workspace/alpha_quant`
- 单次命令建议包 `timeout 600`，避免网络卡顿挂死

## 4. 替代方案对照（去 API key 化）

| 原组件 | 依赖 | 替代为 | 依据 |
|---|---|---|---|
| `TushareLoader` | TUSHARE_TOKEN | vibe `fetch_market_data`（tencent 链首，免 token，前复权 qfq） | 回退链 tencent→mootdx→eastmoney→baostock→akshare→tushare，任一可用即出数 |
| `BacktestEngine`（Backtrader） | backtrader 包（未安装） | vibe `backtest.runner` + `ChinaAEngine` | ChinaAEngine 内置 T+1、100 股整手、涨跌停、佣金；ETF 免印花税须显式 `stamp_tax=0`（已内置） |
| 研报爬虫 `crawler.py` | DeepSeek key（保留） | 可选 `mx-finance-search` skill 或人工投喂 | 情感分数缺省 0，主链路不阻塞 |

### 4.1 免 token 回测的实现机制（loader cache 注入）

vibe runner 的引擎路由按 `source` 字段选择执行规则：只有
`source ∈ {tushare, akshare}` 才路由到 ChinaAEngine；`local` 会错误地落到
CryptoEngine。因此本工作流：

1. config 声明 `source=CACHE_SOURCE`（**值为 `akshare`，不是 tushare**，原因见
   §6.6），只为拿到 A 股交易规则引擎
2. 但数据不走 akshare API：`VibeBacktestExporter.inject_cache()` 把本地 CSV
   按 vibe 的缓存格式（sha256 content-addressed parquet + metadata json，
   version=4）写入 `data/vibe_cache/akshare/<key>.parquet`
3. runner 子进程设置 `VIBE_TRADING_DATA_CACHE=true` +
   `VIBE_TRADING_DATA_CACHE_ROOT=data/vibe_cache`，akshare loader 的
   `cached_loader_fetch` 在触碰 API 前先查缓存——全命中，token 永远不需要
4. 缓存 key 已与 vibe 原版 `make_loader_cache_key` 做逐字节一致性验证

**关键**：缓存 key 的日期区间必须与 config 的 `[start_date, end_date]` 一致
（否则 runner 查不到），但**缓存帧的内容可以更长**——
`cached_loader_fetch` 命中后直接 `return cached`，不做日期裁剪。正是这一点
让 RSRS 能在评估首日就拿到 M=600 的 warmup 窗口（见 §6.8）。

### 4.2 src 包名冲突的子进程隔离

vibe-trading 安装的顶层包 `src`/`backtest` 与本项目 `src` 同名。所有
vibe 子进程（broker、runner）均以 **cwd=$HOME** 运行，此时 `import src`
唯一命中 site-packages 的 vibe 版本。本项目 `runs/` 通过
`VIBE_TRADING_ALLOWED_RUN_ROOTS` 环境变量加入 runner 的运行目录白名单。

## 5. 关键参数备忘

| 参数 | 默认 | 说明 |
|---|---|---|
| `-n` | 18 | RSRS 回归窗口（Design P1-03） |
| `-m` | 600 | beta 标准化窗口；min_periods=60（历史不足时渐进生效） |
| `--top-k` | 2 | 轮动持仓数（等权 1/k） |
| `--min-score` | 0.0 | 入选最低 RSRS 修正分 |
| `stamp_tax` | 0 | **ETF 免印花税**（ChinaAEngine 默认 0.0005 仅适用股票） |
| `commission_rate` | 0.00025 | 佣金万 2.5，最低 5 元 |
| 数据缓冲 | 评估起点前 3 年 | RSRS M=600 lookback 所需 warmup |

## 6. 已修复的历史 bug（本次集成中发现）

1. **RSRS 索引对齐 bug**（`factors/rsrs.py`）：`beta_series` 原以默认
   RangeIndex 创建，与 DatetimeIndex 的 DataFrame 做列赋值/乘法时索引
   无法对齐，导致 `rsrs_zscore`/`rsrs_score` **整列静默 NaN**——即原实现在
   任何真实（日期索引）数据上都不产生信号。已修复并加注释。
2. **`rsrs_zscore` 语义分裂**：`factors/rsrs.py` 存 raw z、
   `rsrs_momentum.py` 存修正分 z×R²。已统一为修正分（RSRS-Right 标准），
   `rsrs_momentum.py` 改为薄别名。
3. **测试构造签名不匹配**：`tests/test_rsrs.py` 以 `RSRSCalculator(n=, m=)`
   构造，canonical 类原不接受参数，测试从未跑通过。已对齐构造签名，
   `tests/test_rsrs.py` 2/2 通过。
4. **main.py 顶部 `import redis`**：redis 包未安装导致任何 CLI 命令直接
   ImportError。已改为 monitor 命令内惰性导入。

以下 5–9 项在 WSL 端到端首跑中发现并修复（均为 vibe 侧或集成侧的坑）：

5. **`--codes` 参数重复注册**（`main.py`）：`common_factor_args` 内含 `--codes`，
   同时 `backtest`/`run_all` 子命令又各自注册了一次，argparse 抛
   `ArgumentError: conflicting option string`。已拆成只含 `-n/-m` 的
   `rsrs_window_args`，`--codes` 由各子命令自行注册。
6. **缓存通道不能用 tushare**（`vibe_exporter.py`）：vibe 的 registry 只把
   `is_available()` 为真的 source 纳入回退链，而 `tushare.is_available()`
   要求配置 TUSHARE_TOKEN。无 token 时 tushare 被直接跳过（日志
   `tushare is unavailable, falling back to akshare`），注入的缓存永远读不到，
   引擎真的去调 akshare 拿**不复权**数据——实测 512480 同日收盘
   0.352（前复权）vs 0.703（不复权），volume 差 100 倍。改为注入
   `akshare` 通道（`is_available()` 仅检查包是否安装，恒真），
   数据本身仍是 tencent 前复权。
7. **`position_adjustment` 必须填 `hold`**（原为 `rebalance`）：rebalance 模式
   每天把持仓拉回目标权重并对整篮做严格资金预检
   （`projected_capital < -1e-9` 即抛 `insufficient capital for position
   rebalance`）。ETF 轮动遇到 T+1 时当日买入的仓位无法卖出
   （`can_execute=False` → reductions 为空），新标的开仓成本仍在，必然资金
   为负而崩溃。hold 模式只在方向变化时平仓重开，且买不起时按统一比例缩放
   篮子。
8. **`evaluation_start_date` 在 0.1.14 不存在**：该字段属于
   `~/source/Vibe-Trading` 的新版源码，已安装的 0.1.14 schema 里没有，
   因 `model_config = ConfigDict(extra="allow")` 而被**静默忽略**。后果是
   回测从 `start_date`（含 warmup）起就交易，RSRS 窗口不足时信号失真——
   实测 2022 年产生 **-42.58%** 的污染收益，把总收益从 +182.78% 拉低到
   +70.81%。解法两层：① config 的 `start_date` 直接取评估起点；
   ② signal_engine 加 `EVAL_START` 门槛，早于该日的权重一律置 0（warmup
   空仓）。修复后 2022/2023 收益归零。
9. **`PROJECT_ROOT` 层级算错**：`vibe_exporter.py` 与 `signal_generator.py`
   用了 `parents[3]`（文件实际在 `src/backtest/`、`src/strategies/`，应为
   `parents[2]`），导致数据目录解析成 `workspace/data/` 而非
   `workspace/alpha_quant/data/`。前者直接触发 FileNotFoundError，
   后者是未被引用的死代码（故一直没暴露）。
10. **vibe tencent loader 的 500 根静默截断**（vibe 侧 bug，本项目已在
    broker 层绕开）：腾讯 `fqkline` 接口单次最多 500 根，且**窗口内 K 线数
    超过 500 时返回的是「end_date 往前 500 根」**（末尾对齐），并非从
    start_date 起算。vibe 的 `_fetch_one` 分页却假设首页从 start_date 开始，
    翻页时 `next_start > end_date` 立即 break，于是任何 >500 根的请求都静默
    退化为最近 500 根。实测：请求 2024-01-01~2026-06-30 返回 500 根且
    first=2024-06-06；`count` 传 >500 则直接返回空。
    另测：1 年窗口（242 根）正常从头返回。
    **绕法**：`scripts/vibe_fetch_broker.py` 按 `--slice-days`（默认 365 天）
    把区间切成多段分别请求后拼接去重，实测 2021-2026 可拿到 1327 根。
    RSRS 的 M=600 必须有这个绕法，否则数据永远只有 500 根。
11. **`fetch_market_data` 返回 `list[dict]` 而非 DataFrame**，且日期字段名为
    `trade_date`（不是 `date`）。broker 里 `coerce_frame()` 负责归一化
    （兼容 list[dict] / DataFrame / cap_rows 截断后的 dict 三种形态）。

## 7. 验证情况

### 7.1 静态验证（Windows 侧）

- [x] 全部新/改文件 py_compile 通过
- [x] RSRS beta/r2/zscore 与 polyfit/corrcoef ground truth 误差 < 1e-9
- [x] 缓存 key 与 vibe 原版实现逐字节一致（含日期 normalize 边界）
- [x] signal_engine 模板通过 AST 沙箱规则静态检查（无顶层可执行语句、
      类体仅常量与方法、无 eval/exec/open/subprocess）
- [x] `tests/test_rsrs.py` 2/2 通过

### 7.2 WSL 端到端实跑（已通过）

`run_all` 四阶段全绿，样本 `512480.SH / 513100.SH / 588000.SH`，
数据 2021-01-04 ~ 2026-06-30（1370 根/标的）：

| 阶段 | 结果 |
|---|---|
| `fetch_data` | 6 段拼接，3 标的各 1370/1370/1371 行，全部 `via tencent`（前复权） |
| `calc_factors` | 每标的 754~755 个有效 RSRS 点（符合 1370−600−18+2） |
| `gen_signals` | 输出 `output/signals_YYYYMMDD.json`（截面排名 + top_k 持仓） |
| `backtest` | ChinaAEngine 跑完，产出 equity/fills/trades/risk_xray 等 13 个 artifact |

**评估窗口指标**（`runs/<name>/evaluation_metrics.json`，按 `eval_start` 切片重算）：

| 指标 | 值 | 说明 |
|---|---|---|
| 区间 | 2024-01-02 ~ 2026-06-30 | 601 bars ≈ 2.38 年 |
| 总收益 | **+182.78%** | 10 万 → 28.28 万 |
| 年化收益 | **+54.63%** | 全区间口径只有 21.81%（被 2 年 warmup 空仓稀释） |
| 夏普 | **1.52** | 全区间口径 1.02 |
| 最大回撤 | −27.33% | Calmar 2.00 |
| 基准收益 | +176.59% | 超额仅 **+6.20%** |

⚠️ **诚实结论**：年化 54.63% 看着漂亮，但同期买入持有三个 ETF 的基准也有
+176.59%，**RSRS 轮动的超额只有 +6.20%**。这套参数（top_k=2、min_score=0、
每日截面轮动）基本没有战胜简单持有，只是因为 2024-2026 这段本身是大牛市。
要判断策略有效性，需换更长的评估窗口、加入熊市样本、并对照等权基准做
显著性检验（vibe 自带 `monte_carlo_test` / `bootstrap_sharpe_ci` 可用）。

### 7.4 滑动窗口回测（7 窗 × 3 策略，已通过）

`sliding_backtest --name sliding_20260831`（2023-01~2026-06，6 个月窗 × 7 段，
21/21 run 成功，结果 `runs/sliding_20260831/summary.md`）：

| 窗口 | 区间 | rsrs_rotation | rsrs_timing | equal_weight |
|---|---|---|---|---|
| 01 | 2023H1 | +7.17% | +4.88% | +15.53% |
| 02 | 2023H2 | +1.26% | −5.51% | −5.72% |
| 03 | 2024H1 | +0.21% | +3.23% | −0.78% |
| 04 | 2024H2 | +17.61% | +13.70% | +31.29% |
| 05 | 2025H1 | −12.60% | +0.45% | +4.12% |
| 06 | 2025H2 | +37.42% | +22.07% | +31.13% |
| 07 | 2026H1 | +86.66% | +49.68% | +55.79% |

| 策略 | 跨窗复利 | 最差单窗 | 平均夏普 | 最深回撤 | 平均超额 | 超额胜率 |
|---|---|---|---|---|---|---|
| rsrs_rotation | +186.74% | −12.60% | 1.04 | −27.34% | +0.92% | 57% |
| rsrs_timing | +113.49% | −5.51% | 0.99 | −12.22% | −6.12% | 29% |
| equal_weight | **+201.80%** | −5.72% | **1.23** | −18.74% | +0.01% | 43% |

⚠️ **结论（与 §7.2 互相印证，且更扎心）**：
1. **被动等权（+201.80%、夏普 1.23）跑赢两个主动策略**——三年半里「什么都不做」
   是最优解，RSRS 轮动只是在 w07（2026H1）一波大幅反超（+86.66% vs +55.79%），
   但 w04/w05 又大幅落后，择时版全程负超额（−6.12%、胜率 29%）。
2. 轮动的价值体现在**回撤与波动 trade-off** 上：最深回撤 −27.34% 确实比
   equal_weight 的 −18.74% 更差，并没有换来风控优势。
3. 当前参数下 RSRS 策略**未证明有效**。下一步见 §9。

**本次发现的第 12 个 bug**（已修）：`write_evaluation_metrics` 原本只裁
`eval_start` 不裁 `eval_end`，而 vibe 的回测轴是注入帧全长（2020→2026），
导致「窗口收益」实际算成「窗口起点→帧末」（w01 曾报 +187%）。现双边裁剪，
并自算窗口内等权买入持有基准替代引擎的全帧内置基准（equal_weight 各窗
超额 ≈0 验证了口径正确）。

### 7.5 参数扫描（4 组调参 × 7 窗，已通过）

默认参数跑输等权后，用同一框架扫了 4 组调参（结果
`runs/param_sweep_20260831.md`，按跨窗复利降序）：

| 配置 | 参数 | 跨窗复利 | 最差单窗 | 平均夏普 | 最深回撤 | 平均超额 | 超额胜率 |
|---|---|---|---|---|---|---|---|
| k1_ms0 | top_k=1, ms=0, 日频 | **+316.34%** | −10.47% | 1.45 | −24.94% | +8.48% | 57% |
| weekly_k2_ms05 | top_k=2, ms=0.5, **周频** | +298.17% | **−3.88%** | **1.51** | −20.79% | +5.16% | **71%** |
| k1_ms05 | top_k=1, ms=0.5, 日频 | +241.39% | −16.91% | 1.13 | −20.46% | +4.28% | 43% |
| 被动等权 | 对照 | +201.80% | −5.72% | 1.23 | −18.74% | +0.01% | 43% |
| baseline | top_k=2, ms=0, 日频 | +186.74% | −12.60% | 1.04 | −27.34% | +0.92% | 57% |
| k2_ms05 | top_k=2, ms=0.5, 日频 | +162.46% | −15.80% | 0.85 | −20.46% | −1.29% | 57% |
| timing | 滞回择时 ±0.7 | +113.49% | −5.51% | 0.99 | −12.22% | −6.12% | 29% |

**结论（部分翻案）**：
1. **调参后 RSRS 轮动能跑赢被动等权**——top_k=1 集中持仓（+316%）与
   周频调仓（+298%）都明显超过等权的 +202%，默认参数（k2/ms0/日频）
   恰恰是全场最差之一。
2. **周频版综合质量最高**：夏普 1.51 全场第一、最差单窗仅 −3.88%、
   超额胜率 71%（7 窗 5 正）。降换手 + min_score 空仓过滤确实有效。
3. top_k=1 收益最高但回撤 −24.94%，w07 单窗 +112% 说明其收益高度依赖
   牛市爆发期押中单一最强标的，稳健性不如周频版。
4. ⚠️ **过拟合警告**：扫参是在同一批数据上挑最优参数，存在选择偏差；
   且样本只有 7 个窗口、3 只 ETF、2023 后全牛市。最优参数样本外未必
   保持——下结论前必须做显著性检验（§9）。

**新增策略模板** `rsrs_rotation_weekly`：由日频模板派生（import 时 assert
锚点唯一，防两份 RSRS 核心漂移），每周首个交易日调仓、目标权重持有整周。

### 7.6 显著性检验（vibe validation 三件套 + 配对检验，已通过）

对扫参最优的两个配置 + 等权对照跑全区间（2023-01-03~2026-06-30）检验
（`runs/validation_20260831.md`，权益曲线按评估窗口切片）：

| 策略 | 夏普 | Bootstrap 95% CI | P(夏普>0) | MC p(夏普) | WF 一致性 |
|---|---|---|---|---|---|
| weekly_k2_ms05 | **1.70** | [0.67, 2.58] | 99.9% | 0.161 | 71% |
| k1_ms0 | 1.46 | [0.43, 2.37] | 99.7% | 0.758 | 86% |
| equal_weight | 1.48 | [0.35, 2.58] | 99.7% | 0.644 | 71% |

滑动窗口超额配对检验（n=7，基准=窗口等权买入持有）：

| 策略 | 平均超额 | 正超额窗 | t 统计量 | 显著性 |
|---|---|---|---|---|
| weekly_k2_ms05 | +5.16% | 5/7 | 1.85 | **p<0.10** |
| k1_ms0 | +8.48% | 4/7 | 0.93 | 不显著 |
| baseline_k2_ms0 | +0.92% | 4/7 | 0.15 | 不显著 |
| timing | −6.12% | 2/7 | −2.21 | **显著为负 (p<0.05)** |

⚠️ **诚实判读**：
1. **周频版是唯一有边际统计信号的主动策略**：Sharpe 1.70 vs 等权 1.48，
   超额 t=1.85（p<0.10）。但其 Bootstrap CI [0.67, 2.58] 包含等权的 1.48，
   **无法在 95% 置信下宣称显著优于等权**——结论是「有积极迹象，未达显著」。
2. **k1 的高收益是波动堆出来的**：+317% 总收益但 Sharpe 与等权持平（1.46
   vs 1.48），超额 t=0.93 不显著——押注单一标的的方差太大。
3. **timing 显著为负（t=−2.21），正式淘汰。**
4. MC p 值普遍不显著：交易顺序对结果影响不大（少交易策略该检验功效弱）。
5. 根本约束仍是样本：3.5 年 / 3 只 ETF / 全牛市，n=7 的配对检验功效很低。
   要真正下结论需要更长历史 + 更大候选池（§9）。

### 7.7 扩展池验证（12 只 × 13 窗，翻案 §7.6）

按 §7.6 的约束把候选池从 3 只扩到 12 只、数据前推到 2017-09（覆盖 2018 熊市
与 2021-2022 下跌），重跑滑动窗口 + 参数扫描 + 显著性检验
（`runs/pool12_review_20260831.md`、`runs/pool12_validation_20260831.md`）。

**候选池**（D 池去 512000 券商，规避与 512880 证券 ρ=0.994 的重复计价）：
510300/510500/159915（宽基）、512010/159928/512880/512660/512400（行业）、
513100/513500/513050（QDII）、518880（黄金）。共同起点 2017-09，13 个可用半年窗。

**滑动窗口横评（13 窗 × 各配置）——结论反转**：

| 配置 | 跨窗复利 | 平均超额 | 超额胜率 | 最差单窗 |
|---|---|---|---|---|
| equal_weight（基准） | **+66.48%** | −0.15% | 38% | −10.56% |
| weekly k4/ms0.0 | −5.44% | −4.27% | 23% | −16.79% |
| weekly k2/ms0.5 | −6.04% | −4.00% | 31% | −18.22% |
| weekly k6/ms0.5 | −16.75% | −5.04% | 23% | −18.54% |
| weekly k8/ms0.5 | −19.01% | −5.31% | 23% | −17.98% |
| weekly k4/ms0.5 | −25.65% | −5.79% | 31% | −21.91% |
| 日频 rotation k2/ms0.5 | −32.64% | −6.63% | 8% | −20.49% |

**显著性检验 + 成本归因（全区间 2020-01-02~2026-06-30，权益曲线切片）**：

| 策略 | 净收益 | 毛收益 | 手续费 | 成本侵蚀 | 夏普 | Bootstrap 95% CI | 超额 t(n=13) |
|---|---|---|---|---|---|---|---|
| equal_weight | +77.63% | +77.76% | ¥125 | 0.13% | 0.599 | [−0.18, 1.41] | −1.59 |
| weekly k2/ms0.5 | −3.15% | +3.54% | ¥6689 | **6.69%** | 0.092 | [−0.70, 0.89] | −1.64 |
| 日频 rotation | — | — | — | — | — | — | **−3.00 (p<0.025 显著为负)** |

⚠️ **结论（正式翻案 §7.6）**：
1. **扩展池上，所有 RSRS 主动策略的窗口超额均为负**——§7.6 的「周频版唯一
   保留候选」是 3 只 ETF / 7 窗纯牛市的**选择偏差**，样本一扩大即失效。
2. **周频版毛收益 +3.54% 是正的，但被 6.69% 手续费吃成 −3.15%**：284 笔交易、
   265 次调仓、总换手 152.88，成本是扩展池上主动策略的头号杀手。等权只花
   ¥125（0.13%）。
3. **日频 rotation 显著为负（t=−3.00，p<0.025）**，正式淘汰。
4. 等权被动组合是唯一正收益项（+77.63% 净 / 0.599 夏普），但其超额 t=−1.59
   不显著（它就是基准本身，超额 ≈ −手续费）。
5. 成本归因揭示：**RSRS 在扩展池上不是「信号失效」单点问题，而是「信号偏弱 +
   换手过高」叠加**——信号毛收益 +3.54% 尚可，但周频调仓频率在 12 只池上
   制造了 6.7% 的年化级成本拖累。下一步应优先降频/加空仓门槛（§9）。

### 7.8 降频验证（月频调仓，推翻「降频可救」假设）

针对 §7.7 结论 5 的「降频压换手」方向，新增 `rsrs_rotation_monthly` 模板
（周频模板派生，仅 `to_period("W")→"M"`），在 12 只池上重跑 13 窗滑动窗口
（`runs/sliding_pool12_monthly/`），并与周频做逐窗成本归因
（`scripts/compare_cost.py`）。

**换手成本对比（13 窗累计，每窗 10 万本金独立）**：

| 配置 | 累计手续费 | 累计单边换手 | 平均每窗调仓 | 跨窗毛收益 | 跨窗净收益 |
|---|---|---|---|---|---|
| 周频 k2/ms0.5 | ¥50912 | 1001 倍 | 156 次 | **+56.63%** | −6.04% |
| 月频 k2/ms0.5 | ¥17520 | 345 倍 | 54 次 | +11.29% | −6.84% |

**滑动窗口横评（月频 vs 等权）**：

| 配置 | 跨窗复利 | 平均超额 | 超额胜率 | 最差单窗 |
|---|---|---|---|---|
| equal_weight（基准） | **+66.48%** | −0.15% | 38% | −10.56% |
| 月频 k2/ms0.5 | −6.84% | −3.77% | 31% | −25.33% |

⚠️ **结论（否定「降频即可救」）**：
1. **降频确实把成本砍了 2/3**：手续费 ¥50912→¥17520、换手 1001→345 倍、
   调仓 156→54 次/窗——月频在成本端完全奏效。
2. **但净收益没改善，反而略差**：−6.04% → −6.84%。因为**毛收益同步崩塌**
   +56.63% → +11.29%——RSRS 是动量型信号，其 alpha 高度依赖调仓频率，
   降频在省成本的同时把信号也钝化了。
3. **决定性事实**：周频毛收益 +56.63% 已是所有主动配置的天花板，仍**低于**
   等权 +66.48%（等权几乎零成本）。即「即使零手续费，RSRS 轮动也跑不赢
   被动等权」——**信号 alpha 本身不足，与成本无关**。
4. 至此 RSRS 截面轮动在扩展池上被**彻底证伪**：三档频率（日/周/月）、
   五档 top_k（1/2/4/6/8）、多档 min_score 全部跑输等权。§9 据此收口。

### 7.9 因子有效性诊断（截面 IC + 分层收益，定位失效根因）

针对 §7.8「信号 alpha 本身不足」的结论，用标准因子检验定位根因
（`scripts/factor_diagnosis.py`，扩展池 12 只、共同样本 2017-09 起、2700+ 观测）：

**截面 IC（Spearman rank IC，RSRS 修正分 vs 未来 k 日收益）**：

| 未来收益 | IC 均值 | ICIR | IC 标准差 | 正 IC 占比 |
|---|---|---|---|---|
| 5 日 | −0.0132 | −0.035 | 0.376 | 49.5% |
| 10 日 | −0.0186 | −0.049 | 0.381 | 49.0% |
| 20 日 | −0.0109 | −0.028 | 0.388 | 50.0% |

**分层收益（每交易日按修正分 5 分组，各组未来收益均值）**：

| 未来收益 | Q1(低) | Q2 | Q3 | Q4 | Q5(高) | 多空 Q5−Q1 | 单调？ |
|---|---|---|---|---|---|---|---|
| 5 日 | +0.14% | +0.20% | +0.18% | +0.19% | +0.18% | +0.04% | 否 |
| 10 日 | +0.23% | +0.42% | +0.43% | +0.38% | +0.34% | +0.11% | 否 |
| 20 日 | +0.58% | +0.90% | +0.73% | +0.77% | +0.48% | **−0.09%** | 否 |

⚠️ **结论（定位到根因：因子失效，非池子同质化）**：
1. **IC 均值全为负且近零**（−0.01 ~ −0.02），ICIR 绝对值 < 0.05——学界惯例
   |ICIR|>0.5 才视为可用因子，此处**低一个数量级**，截面无预测力。
2. **正 IC 占比 ≈ 50%**——与抛硬币无异，RSRS 修正分对未来收益方向**零区分度**。
3. **分层收益完全不单调**，多空收益在 ±0.1% 内抖动、20 日甚至为负——
   高分标的不比低分标的涨得多。排除了「池子同质化稀释信号」的假设：
   若因子有效只是被同质化稀释，至少应保留弱单调性；实际是**纯噪声**。
4. **根因链闭合**：RSRS（N=18/M=600 的 z×R2）在扩展池上无截面 alpha →
   毛收益天花板 +56.63% 纯属 beta（追涨赶上牛市）→ 叠加换手成本必然跑输等权。
5. **最终判定**：RSRS 截面轮动作为 AlphaQuant 的核心主动策略，**证伪成立**。
   被动等权是当前唯一站得住的基线；RSRS 仅可保留作为「上升趋势强弱」的
   描述性指标，不具选股/轮动决策价值。

### 7.10 新因子批量筛选（16 个价量因子全灭，证伪收口）

为回答 §9「换新因子」，新增因子库 `src/strategies/factors/factor_library.py`
（动量/波动/趋势/量价/风险调整 5 族 16 因子）与批量筛选脚本
`scripts/factor_screening.py`，在扩展池 12 只上跑与 §7.9 相同的截面
IC + ICIR + 分层收益三件套（向量化实现，16 因子 × 3 视界 × 120 相关对
约 3 秒跑完）。判定门槛：强通过 |ICIR|≥0.5、弱通过 |ICIR|≥0.3，且正 IC
占比须落在 [45%,55%] 之外。

**因子排名（按 best |ICIR| 降序，完整表见 `runs/factor_screening_20260831.md`）**：

| 因子 | 族 | bestH | bestICIR | 正IC% | 多空20d | 判定 |
|---|---|---|---|---|---|---|
| max_dd_60 | 波动 | 20 | **+0.238** | 61.5% | +1.25% | FAIL |
| dollar_vol_20 | 量价 | 20 | −0.214 | 42.5% | −0.94% | FAIL |
| downside_vol_60 | 波动 | 20 | −0.195 | 41.3% | −0.90% | FAIL |
| vol_60 | 波动 | 20 | −0.194 | 40.1% | −1.18% | FAIL |
| mom_240_20 | 动量 | 20 | +0.172 | 57.8% | +0.72% | FAIL |
| vol_20 | 波动 | 20 | −0.135 | 43.2% | −0.90% | FAIL |
| pv_corr_20 | 量价 | 20 | −0.093 | 45.6% | −0.29% | FAIL |
| 其余 9 因子 | — | — | \|ICIR\|<0.07 | ≈50% | ≈±0.4% | FAIL |

⚠️ **结论（16/16 全灭）**：
1. **无一因子通过门槛**。最高 |ICIR| = 0.238（max_dd_60），仍低于弱通过
   0.3，更遑论 0.5。动量族（mom_*）与趋势族（ma_*）ICIR 普遍 < 0.1，
   与 RSRS（ICIR −0.03）同量级，均为纯噪声。
2. **相对最强的是「波动/风险族」与「流动性族」**：vol_60 / downside_vol_60 /
   dollar_vol_20 的负 IC（低波/小盘溢价方向）勉强可见但极弱，且正 IC 占比
   仅 40~43%、分层不单调——不足支撑轮动决策。
3. **经典 12-1 动量（mom_240_20）在 A 股 ETF 池上失效**（ICIR +0.172，
   多空 +0.72% 且不单调），与学界「A 股短周期反转、长周期动量弱」的结论
   一致。
4. **根因收敛（待 §7.11 检验）**：结合 §7.9 与本次 16 因子全灭，初步判断
   **问题不在某个因子构造，而在池子本身**——12 只 ETF 中 10 只是高度相关的
   权益类（PC1=57.5%），截面价量差异不足以产生可交易的截面 alpha。§7.9 曾
   「排除同质化假设」，本次 16 因子系统性失效后须**修正该结论**：RSRS 是纯
   噪声固然成立，但更底层的约束可能是**候选池同质化**。这一「同质化」假设
   是否成立，须用异构池（跨资产/跨市场）复跑来检验——见 §7.11。

### 7.11 异构池检验（跨资产/跨市场 18 只，仍全灭 → 同质化假设被证伪）

为检验 §7.10「根因是池子同质化」的假设，用 `vibe_fetch_broker.py` 拉取 6 只
异构资产（债券 511010 国债/511180 可转债、商品 159985 豆粕/159981 能源化工、
跨境 513030 德国30/159920 恒生），与既有 12 只拼成 **18 只跨资产/跨市场池**
（A股宽基 3 + A股行业 4 + 债券 2 + 商品 3 + 跨境权益 6）。池子分散度实测提升：

| 池 | 只数 | 共同样本 | 平均ρ | 最高ρ | PC1 | 有效因子(90%) |
|---|---|---|---|---|---|---|
| 同质池 | 12 | 2399 日 | 0.422 | 0.851 | 66.1% | 4 |
| 异构池 | 18 | 2790 日 | **0.289** | 0.871 | 68.6% | 3 |

平均相关从 0.422 降到 0.289（债券/商品确实更独立），但 **PC1 仍高达 68.6%**
——权益块（13/18）主导总方差，加进来的债券/商品是「各自独立但方差占比小」，
有效独立因子数不升反降（4→3）。

**同期（2020-09-01 起）对照，消除样本期混叠后的关键因子 ICIR**：

| 因子 | 同质池12 | 异构池18 | 变化 | 判定 |
|---|---|---|---|---|
| mom_240_20（12-1动量） | +0.288 | +0.289 | 持平 | FAIL |
| dollar_vol_20（流动性） | −0.122 | −0.213 | ↑ 变强 | FAIL |
| ma_ratio_20（短期趋势） | +0.105 | +0.164 | ↑ 变强 | FAIL |
| max_dd_60（回撤反转） | +0.280 | +0.181 | ↓ 变弱 | FAIL |
| vol_60（低波） | −0.260 | −0.147 | ↓ 变弱 | FAIL |

⚠️ **结论（同质化假设被证伪，结论更深一层）**：
1. **异构化没有救活任何因子**。最强因子（12-1 动量 mom_240_20）在两池几乎
   完全一致（+0.288 vs +0.289），仍卡在 0.3 门槛之下。也就是说，**问题不是
   池子同质化，而是「日频价量截面因子」本身在 ETF 宇宙里没有可交易 alpha**。
2. **异构化的边际效果是分化而非提升**：它确实增强了「流动性/规模」（dollar_vol_20
   −0.122→−0.213，跨资产规模差异是真实暴露）和「短期趋势」（ma_ratio_20），
   却稀释了「低波/回撤反转」（vol_60、max_dd_60 变弱——债券低波、商品异波动
   搅乱了权益波动信号）。净效应≈零。
3. **2020-09+ 的 ICIR 普遍高于全期**（mom_240_20 从 +0.172→+0.288）：这个时段
   恰好是「A股熊 + 商品牛 + 美股牛」的高截面离散期，是价量因子最有利的样本，
   即便如此仍无因子过线——反证价量因子天花板极低。
4. **最终判定（§7 收口）**：日频 OHLCV 能构造的截面价量因子（动量/波动/趋势/
   量价/风险调整 16 个 + RSRS）在 ETF 宇宙上**全部证伪**。下一步 alpha 只能来自
   价量数据之外的维度：carry/价值（需基本面数据）、时序择时/轮动（股金、趋势、
   波动率择时）、或结构套利（QDII 溢价）。见 §9。

### 7.12 时序择时 + 大类资产轮动（修正前视偏差后，时序路线也证伪）

§7.11 收口时把「时序择时/轮动」列为价量维度最后的 alpha 希望。用两个新脚本
在 18 只异构池上检验：`scripts/timing_diagnosis.py`（趋势跟踪，多/空现金）与
`scripts/rotation_test.py`（股/债/金动量轮动）。

**1. 趋势跟踪多/空现金（timing_diagnosis.py）——无 alpha**：mom_60/120/240、
ma_200、ma_50_200 五种趋势信号，>0 做多、否则空仓现金，逐标的对比 buy-hold。
池化 Δ夏普全部 ≤0（ma_200 最好 −0.019），改善标的占比最高 56%（ma_200）；
时序溢价（多头日收益 − 空仓日收益）≈0 或负。ma_200 的「改善」来自降回撤
（暴露约 50%）而非增 alpha；vol_target（波动率倒数缩放仓位）池化 Δ夏普 +0.018，
是风险预算而非 alpha 来源。

**2. 大类资产轮动（rotation_test.py）——前视偏差修正后无 alpha**：把「空仓=
现金」换成「切债/切金/多资产动量 top1」，四种策略对比如下（修正后，全区间）：

| 策略 | 夏普 | 年化收益 | 最大回撤 |
|---|---|---|---|
| equal_weight_18（基线） | 0.60 | +9.21% | -26.0% |
| equity_bond（破 MA200 切国债） | 0.55 | +8.46% | -21.9% |
| equity_gold（破 MA200 切黄金） | 0.76 | +13.32% | -24.2% |
| mom3_egb（股/金/债动量 top1） | 0.60 | +9.93% | -24.9% |
| mom4_egbn（+纳指四资产） | 0.50 | +10.30% | -35.5% |

equity_gold 的 0.76 看似超基线，但低于纯黄金 buy-hold（0.78），本质是黄金
beta 而非策略 alpha；mom3 恰等于基线、mom4 反而跑输（回撤扩大到 −35.5%）。

⚠️ **前视偏差陷阱（本轮最重要教训）**：首版 `momentum_rotation` 未对动量信号
`.shift(1)`，用 t 日收盘动量直接乘 t 日收益，把 mom3_egb 夏普虚高到 1.34、
mom4_egbn 到 1.64（年化 +32.86%），一度误判「轮动是唯一正 alpha 路线」。
加 `.shift(1)`（与 equity_bond 的 `up.shift(1)` 一致）后全部打回原形。这正是
§7.9「全区间亮眼、样本外全灭」的机制根源——**任何信号必须用 t-1 及之前信息
决定 t 日持仓，逐行核对 shift**。

**3. 最终收口（§7 完结）**：价量数据在 ETF 宇宙上的三条主动 alpha 路线——
截面因子（16 个 + RSRS）、时序趋势择时（多/空现金）、大类资产轮动（切债/金/
动量 top1）——**全部证伪**。修正前视偏差后无任一主动策略夏普超过被动等权基线
（0.60）。仅有的两个更高夏普「策略」是纯被动持有黄金（0.78）和国债（1.37），
那是资产配置（beta）不是 alpha。**价量维度 alpha 天花板已彻底探明：≈0**。

### 7.13 基本面/估值因子（carry/value）——快照诊断 + 数据覆盖局限

§9 第一项（跳出价量维度）。新增 `src/data_engine/fundamental_loader.py` +
`src/strategies/factors/fundamental_factors.py` + `scripts/fundamental_screening.py`，
用 akshare 免费数据抓估值快照做截面 carry/value 诊断。

**数据源与覆盖（实测）**：中证指数官方 `stock_zh_index_value_csindex` 给最新
约 20 日 PE + 股息率（快照）；乐咕乐股 `stock_index_pe_lg/pb_lg` 给**月频完整
历史（2005 至今约 250 个月）**，但仅覆盖 3 个宽基（上证50/沪深300/中证500）。
7 只 A 股权益 ETF 中 6 只拿到 PE + 股息率（创业板 399006 因中证官网 404 缺失）。

**估值快照（2026-08-03）**：

| ETF | 指数 | PE(TTM) | 股息率% | 盈利收益率% | ERP%(10Y国债1.69%) |
|---|---|---|---|---|---|
| 512880.SH | 证券公司 | 15.88 | 2.01 | 6.30 | 4.61 |
| 510300.SH | 沪深300 | 16.84 | 2.55 | 5.94 | 4.25 |
| 159928.SZ | 中证消费 | 17.98 | 3.94 | 5.56 | 3.87 |
| 512010.SH | 中证医药 | 24.12 | 1.84 | 4.15 | 2.46 |
| 510500.SH | 中证500 | 29.60 | 1.40 | 3.38 | 1.69 |
| 512660.SH | 中证军工 | 49.92 | 0.56 | 2.00 | 0.31 |

证券/沪深300 最便宜（ERP 最高），军工最贵（ERP 仅 0.31%）。全部权益 ERP 为正
（10Y 国债仅 1.69%，股债性价比偏权益）。

⚠️ **关键局限（诚实记录）**：免费源无「多年 × 多标的」估值面板——中证官网只有
20 日快照（× 6 行业）、乐咕有多年月频但仅 3 宽基，**无法跑多标的截面 IC**。
补全路径：Tushare Pro `index_dailybasic`（需 2000 积分）。§7.16 记录了 token
无权限的实测 + 乐咕月频「时序估值择时」替代检验的结果。

### 7.14 QDII 影子 IOPV 溢价（结构套利，真实数据实装）

§9 第二项。重写 `src/data_engine/qdii_calc.py`（原 mock 的 premium=当日涨跌幅，
完全错误），新增 `scripts/qdii_monitor.py`：

- **官方溢价** = −东财「基金折价率」（`ak.fund_etf_spot_em()` 的 IOPV实时估值
  /基金折价率，QDII ETF 全量覆盖）。
- **影子溢价** = 价格 / (官方IOPV × (1+底层市场最新涨跌幅)) − 1，修正美股/
  港股隔夜跳空（底层用 `index_us_stock_sina` / `stock_hk_index_daily_em`）。

**实测（2026-08-31）**：

| ETF | 官方溢价% | 影子溢价% | 信号 |
|---|---|---|---|
| 513100.SH 纳指 | +10.99 | +11.57 | ⚠️ 溢价偏高 |
| 513500.SH 标普500 | +9.07 | +9.34 | ⚠️ 溢价偏高 |
| 513050.SH 中概互联 | −1.46 | −0.94 | 正常 |
| 513880.SH 日经 | +5.07 | — | — |

纳指/标普500 溢价近 10% 是 QDII 额度告罄的典型现象——这是唯一有「结构套利」
性质、不依赖预测的独立 alpha 信号（溢价 >3% 回避买入，<−3% 折价是套利买入窗口）。
（表中日经影子列「—」为初版无底层源所致，§7.18 精细版已补齐 DAX/日经 + 汇率。）

⚠️ **踩坑记录**：① 东财「基金折价率」符号约定是 `(IOPV−价格)/IOPV`（正值=折价），
须取负才是溢价率，否则 513100 会被读成 −10.94% 折价。② 东财 spot 代码是
6 位裸代码（无 `.SH`），映射时须 `code.split(".")[0]`。③ 恒生接口间歇性
ConnectionError，代码已优雅降级退回官方口径。④（精细版，见 §7.18）德国 DAX/
日经最初无可靠日线源导致影子列留空，后改用 `ak.index_global_hist_sina` 补齐
底层、`ak.currency_boc_sina` 补汇率，6 只 QDII 影子溢价现已全量可算。

### 7.15 被动基线 + 风险平价（兜底输出）

§9 第三项。新增 `scripts/risk_parity.py`：18 只异构池 + 核心三腿（股/金/债）
的等权(月度)/逆波动/逆方差/等风险贡献(ERC) 四种组合，月度再平衡、无前视。

| 组合 | 等权(月度) | 逆波动 | 逆方差 | ERC |
|---|---|---|---|---|
| 18 只池 夏普 | 0.51 | 0.87 | 2.03 | 0.94 |
| 18 只池 换手/年 | 0.18 | 1.45 | 1.15 | 4.33 |
| 核心三腿 夏普 | 0.75 | 1.53 | 1.48 | 1.07 |
| 核心三腿 换手/年 | 0.09 | 0.92 | 0.58 | 5.36 |

**结论**：① 风险平价靠降波动抬夏普（权重压向国债/黄金），非选股 alpha——是
「兜底输出」而非超额。② **逆波动是甜点位**（18 池夏普 0.87 vs 等权 0.51、
核心三腿 1.53 vs 0.75，换手仅 0.9~1.5 倍/年）。③ ERC 夏普略高但换手 3~5 倍，
计入成本后大概率不如逆波动 → **兜底组合选逆波动**。核心三腿 ERC 末月权重
国债 94%/股 3.3%/金 2.7%，验证等风险贡献成立。

⚠️ **踩坑记录**：ERC 用 scipy SLSQP 时，协方差量级 ~1e-4 导致目标函数 ~1e-8，
数值梯度失效返回初始等权——须把目标改为「归一化风险贡献」O(1) 量级（见
`erc_weights` 内注释）。这是继 §7.12 前视偏差后第二个「结果看着正常实则错误」的坑。

### 7.16 基本面全历史接入（第二次尝试）：Tushare token 无权限 + 乐咕时序择时替代

§9 最高优先项「接 Tushare 拿全历史做截面 IC」。用户提供 token 后实测：

**Tushare 权限结论（硬阻塞）**：token 有效但**积分为 0**，连最低门槛的
`trade_cal`/`stock_basic`/`daily` 都返回 40203「接口无权限」；`index_dailybasic`
（指数估值日频历史）需 **2000 积分**。已搭骨架（`fundamental_loader.py` 的
`TS_INDEX_MAP` + `fetch_tushare_index_dailybasic()`），token 补够积分后一条命令
即可拉多年 × 多标的 PE/PB。

**免费源穷举结论**：中证官网 indicator.xls 仅 20 日；乐咕 `index-basic-pe/pb`
仅 3 宽基（上证50/沪深300/中证500）但月频**完整历史（2005 至今）**——上一轮
「近 1 年」的注释是错的，已修正。

**时序估值择时替代检验**（`scripts/fundamental_timing.py`，滚动 5 年 PE 分位 →
未来 12 月收益）：

| 宽基 | Q1便宜 | Q5贵 | 多空Q1-Q5 | 单调递减 | 分段多空(13-17/18-22/23-27) |
|---|---|---|---|---|---|
| 上证50 | +14.7% | −8.7% | +23.4% | 是 | +40.8% / +22.5% / +11.7% |
| 沪深300 | +14.1% | −5.1% | +19.2% | 是 | +42.5% / +8.2% / +7.9% |
| 中证500 | +2.2% | +0.9% | +1.3% | 否 | 大多为空（分位长期不极端） |

**结论**：① 沪深300/上证50 的 **value 因子在时序维度有效且跨时段稳健**（便宜
买入跑赢贵时买入 19~23%，分段多空自 2013 年起稳定为正）——这是继 QDII 溢价后
第二个站得住的信号。② 中证500 无效（多空仅 1.3%，其滚动 PE 分位在 2008-2022
年间从未同时触及高低两端，信号本身不极端）。③ 但**估值择时作为策略收益端跑输
满仓**（沪深300 择时年化 2.3% vs 持有 7.8%），价值在**控回撤**（回撤 −37% vs
−71%）而非增收益；简单三档规则下夏普不升反降。

⚠️ **踩坑记录**：① pandas `DataFrame(dict, index=...)` 按索引对齐 dict 内
Series，RangeIndex 与日期索引对齐失败 → 全 NaN（须 `.to_numpy()` 传裸值）。
② 乐咕返回月频完整历史非「近 1 年」，上一轮把全历史分位误标为「1y 分位」
（已改名 `pe_pct_hist`）。③ 分段分层时某段 Q1/Q5 档可能无观测（如中证500 分位
长期不极端）→ 多空差为 NaN，须跳过而非当成 0。

### 7.17 蛋卷基金 63 指数截面估值排名（免费源扩充快照覆盖）

§7.16 穷举免费源时漏了**蛋卷基金**（danjuanapp.com，雪球旗下）。实测其公开接口
`GET https://danjuanapp.com/djapi/index_eva/dj` 免登录直接返回 **63 个指数**（含
跨境：纳指100/标普500/恒生/德国DAX 等）的当前估值，自带 pe/pb 历史分位（0~1，
越低越便宜）和 `eva_type`（low/mid/high 评级）——比中证官网 20 日 xls（6 A 股）
覆盖广一个量级，替代它做截面估值排名。

已封装 `fetch_danjuan_index_valuation()`（`fundamental_loader.py`）+ 报告脚本
`scripts/fundamental_snapshot_report.py`，输出 `runs/fundamental_snapshot.md`
/ `.json` + `data/fundamental/danjuan_valuation.csv`。

**关键发现**（2026-08-28 截面）：
- 自选池里 **中证消费（PB 分位 2.3%）、证券公司（19.9%）最便宜**；沪深300（47.7%）
  /中证500（78.8%）/创业板（63.6%）/军工（53.3%）已偏贵。
- 全市场最便宜：养老产业、中概互联50、国证食品、主要消费（PB 分位 <3%）。
- 全市场最贵：德国DAX（99.8%）、中证煤炭（98.7%）、全指信息/中证电子（97%）。
- 蛋卷评级：低估 12 个、中性 3 个、高估 48 个（当前市场整体估值不低）。

**局限**：① 单截面快照，无法回溯历史，仍不能跑截面 IC；② 蛋卷 `history` 接口需
登录（返回"请重新登录"），历史面板仍待 Tushare 2000 积分或雪球/蛋卷登录 cookie
解锁；③ pe=0（亏损行业如地产）的 PE 分位无意义，以 PB 分位为准；④ 分位基于各
指数自身历史窗口（begin_at 起），跨指数比较分位时窗口长短不一。

### 7.18 QDII 溢价套利回测 + 产品化（定时任务落地）

§9 第二项完整落地，把 §7.14 的「当前截面告警」升级为可回溯、可调度、可验证的
产品化能力。新增 `scripts/qdii_backtest.py`（回测）+ `scripts/qdii_daily.py`
（每日编排），`qdii_calc.py` 补齐 DAX/日经底层 + 汇率（§7.14 精细版）。

**A. 历史溢价序列重构**（免费源，无 token）
- 溢价 = 收盘价 / 单位净值 − 1；收盘价=新浪 `fund_etf_hist_sina`（稳定），
  净值=东财 `fund_etf_fund_info_em`（2018 至今约 8 年）。
- 复权：QDII 有份额拆分（513100 于 2022-01-14 拆分，不复权价单日 −80%），
  收益用「累计净值/单位净值」复权因子折算前复权价，否则收益全错。
- 前视：信号 T 日 → 调仓 T+1 日（`shift(1)`）。

**B. 两条策略回测**（6 只 QDII，2018-2026，全区间年化超额 vs 买入持有）

| 名称 | 溢价均值% | P90% | 基准年化 | 回避年化 | 超额 | 18-23超额 | 24-26超额 |
|---|---|---|---|---|---|---|---|
| 纳指100 | 2.28 | 7.55 | 21.49 | 65.57 | +44.08 | +39.53 | +55.24 |
| 标普500 | 1.36 | 5.40 | 15.82 | 40.40 | +24.58 | +19.48 | +37.38 |
| 中概互联 | 1.54 | 5.03 | −3.96 | 20.41 | +24.37 | +32.20 | +6.70 |
| 德国30 | 0.58 | 2.58 | 6.25 | 24.97 | +18.72 | +15.30 | +27.76 |
| 恒生 | 0.06 | 1.12 | −0.19 | −0.10 | +0.09 | +0.00 | +0.33 |
| 日经225 | 0.74 | 3.06 | 11.99 | 14.61 | +2.62 | +0.39 | +7.21 |

**C. 关键结论（如实）**
- 回避策略超额巨大且跨时段稳定，但来源是「高溢价期=暴跌期」的样本内相关性：
  QDII 溢价高企（纳指溢价 P90 达 7.55%、最大 28%）几乎都出现在恐慌抢购时
  （2020 疫情、2022 加息、2024 日元套息平仓），随后底层下跌，回避恰好躲开。
  这是同期相关性，非可外推的择时能力（样本仅 6 只 × 8 年、少数几次事件）。
- 回避策略的「年化超额」主要是**降回撤的复利效应**：纳指回撤 −28.6%→−17.4%、
  夏普 0.89→3.99。
- 恒生（溢价均值 0.06%，几乎无溢价）回避完全无效（+0.09），反证超额来自溢价
  回归而非择时能力。
- 折价买入（T 日折价<−3% 买，溢价转正卖）是更可信的低波动策略：纳指年化约 9%
  但回撤仅 −1.8%、夏普 1.42，本质是「低仓位 + 折价安全垫」。
- **操作含义**：溢价>3% 是「回避买入/减仓」告警（有价值，但不能做空）；折价<−3%
  是相对可靠的逢低买入窗口。

**D. 产品化落地**
- `scripts/qdii_daily.py`：编排「监控快照 + 回测刷新（--refresh 重拉最新数据点）」，
  每步独立子进程、错误隔离，日志落 `logs/qdii_daily.log`。
- crontab（已安装）：`30 15 * * 1-5` 交易日收盘后自动跑
  `cd ~/workspace/alpha_quant && /usr/bin/python3 scripts/qdii_daily.py`。
- 手动等价命令：`python3 scripts/qdii_daily.py`（或 `--skip-backtest` 只跑监控）。

**E. 2026 年专项补记（信号反转，`scripts/qdii_2026_analysis.py`）**

单独看 2026 单年（01-05 ~ 08-28，8 个月），暴露了与全样本结论相反的结构变化——
**高溢价从「脉冲式恐慌」变成了「额度告罄的结构性常态」，3% 绝对阈值在纳指/标普/
日经上失效**：

- **溢价中枢三连升**（2018-23 → 2024-25 → 2026）：纳指 1.56→2.85→7.05%、
  标普 0.19→3.53→5.17%、日经 −0.05→1.68→3.31%；中概/恒生反而落到微折价
  （−0.46/−0.72%）。纳指全年 94% 时间（148/158 天）溢价 >3%。
- **回避策略 2026 收益漂亮但机制变质**：纳指 +23.26% vs 持有 +17.07%、标普
  +29.68% vs +11.35%、德国 +37.51% vs +2.46%、日经 +42.22% vs +27.75%。但拆开
  看——纳指全年**只持仓 10 天**、标普 26 天，超额全部来自这些「溢价短暂回落」日
  恰好大涨（持仓日均 +2.1%/+1.0%，空仓日日均 ≈0）。这是「错杀大部分日子、侥幸留
  在大涨日」的样本内幸存，不可外推。
- **折价买入窗口 2026 年关闭**：6 只 QDII 全年无一只折价触及 −3%（最深的恒生/中概
  仅 −1.6%），策略零触发、收益 0。
- **中概反证溢价无预测力**：中概互联 2026 买入持有 −26.38%（6 只最差），但溢价均值
  仅 −0.46%、从未 >3%，回避策略空仓 0 天、完全没预警。
- **信号反转的启示**：结构性高溢价时代，「溢价从高位回落」往往意味着底层正在大涨
  （场内价暂时没跟上），更接近**买点**而非危险信号——与原「高溢价回避」相反；真正
  危险的是「溢价从低位突然飙升」（额度骤紧）。可落地修正：监控从「绝对水平告警」改
  为「相对变化告警」（溢价一阶差分 / 偏离中枢 z 值），捕捉「低位→飙升」与「高位→
  回落」两个拐点，而非死守 3% 绝对值。

**F. 相对变化告警 + 全量按年回测（`scripts/qdii_relchange_backtest.py`，§9 升级落地）**

把 E 的「相对变化」启示做成可回测信号：溢价一阶差分滚动 z 分数
`z_t = (dprem_t − mean_{t-W..t-1}) / std_{t-W..t-1}`（W=60 交易日，均值/标准差
滞后 1 期、无前视），两条规则：

- **飙升回避**：z > +2 → 次日空仓，否则持有（替代绝对阈值回避）。
- **回落买入**：z < −2 → 次日买入，z > +2 卖出（逢低买入窗口）。

全量（2018-2026）+ 按年回测结论：

- **飙升回避是「可用的尾部对冲」**：保持 ~95% 暴露、只在溢价 +2σ 飙升的 ~4% 交易日
  空仓，纳指夏普 0.89→1.95、标普 0.84→1.85、德国 0.30→1.01、中概 −0.13→0.18；相比
  绝对阈值回避（纳指暴露仅 ~6%、几乎错杀全部上涨日），几乎不牺牲长期上涨，是可实盘
  叠加的减仓信号。
- **超额仍源于「溢价飙升→短期回落」的恐慌回归，非普适择时**：按年看飙升回避几乎
  年年为正（每年至少一次 spike→drawdown），但恒生/日经（溢价均值 0.06%/0.74%）超额
  ≈0（恒生 +10.5pp、日经 +50pp、夏普几乎不变），反证信号来自溢价本身、非市场择时。
- **阈值敏感性单调（非单点巧合）**：z_hi 1.0→3.0，组合平均年化超额 31.9%→4.2% 单调
  衰减，说明「溢价飙升越极端、次日越差」是稳定排序。
- **回落买入更弱且不稳**：纳指/标普/中概/德国为正但日经转负（累计 −32.9pp）、按年
  波动大，只作「逢低加仓」参考。

落地：`qdii_monitor.py` 主告警已从「绝对水平」升级为「溢价变动 z 分数」（飙升→
回避/减仓、回落→逢低买点），绝对阈值仅作兜底；`qdii_calc.py` 新增
`relchange_zscore()` + `RELCHANGE_WINDOW/RELCHANGE_Z` 常量。

**G. 飙升回避实盘约束版（`scripts/qdii_relchange_realistic.py`）**

F 的「理想版」零成本、可任意空仓/买回，忽略 QDII 申赎摩擦。G 给飙升回避加三大约束，
量化「真实净超额还剩多少」：

1. **触发前提 floor**（premium > floor% 才看 z，默认 1%，过滤低溢价噪声）；
2. **最小持有期 min_hold**（空仓至少 5 交易日才买回，贴合 T+2 到账）；
3. **单边交易成本 cost**（0.15% = 佣金 + 冲击 + 滑点 + 限购摩擦）。

结论（全量 2018-2026，理想超额 → 实盘净超额）：

- **实盘约束不杀信号，只杀「高换手高溢价」的纳指**：标普/德国/中概净超额几乎完整
  保留甚至微升（标普 +16.67→+15.32、德国 +14.02→+15.94、中概 +9.08→+9.38），纳指
  缩水 60%（+22.37→+8.97，因溢价波动最大、换手最高 142 次），日经转负
  （+3.40→−0.78）。
- **floor 净效果为正**：0→3% 组合净超额 +8.5→+5.6 平缓单调无悬崖，floor=1% 过滤
  低溢价噪声后标普/德国净超额不降反微升。
- **min_hold 压降换手（幅度小于初报）**：纳指 134→110 次（8.3 年累计翻转），
  折年 16.15→13.26 次/年（−18%）；恒生本就低频、min_hold 无效果（44→44，
  5.28 次/年）。**口径易错点**：① 这里的次数是「0↔1 翻转」，一次完整
  「减仓+回补」= 2 次翻转，故实盘版折合 **2.6~6.6 个完整回合/年**（纳指 6.6）；
  ② 上表 134/110/44 均为 8.3 年**累计**次数，**不是年化**——早年口头总结的
  「换手降到 4~6 次/年」是把累计次数当成了年化，此处更正。
  min_hold 1→20 净超额 +8.5→+4.2 平缓衰减。
- **成本敏感性温和**：cost 0→0.30% 净超额仅 +10.1→+6.6，信号并非「薄利到碰不得」。
- **修正 F 的隐含乐观**：上一轮「收益被高估 2-3 倍」的担忧偏悲观——真实净超额是
  「纳指被高估、其余基本实」，组合平均净年化超额 +8.4pp（纳指 +8.97、标普 +15.32、
  德国 +15.94），仍可作减仓/对冲信号；但须容忍限购「想买回时买不回」，且 2026 信号
  衰减趋势未变。

### 7.19 value 时序估值择时——完整回测（PB 显著强于 PE，价值宽基可提夏普）

§7.16 的 `fundamental_timing.py` 是「单指标 PE-TTM + 单规则三档」的诊断版，其
「夏普不升反降」结论过于悲观。本节点用 `scripts/value_timing_backtest.py` 做
完整回测：多估值指标（PE-TTM/LYR/中位数 + PB/中位数）、分位窗口敏感性
（3/5/7/10y + 全历史）、Newey-West(lag=12) 重叠样本显著性、多择时规则
（三档/两档/线性×2）、完整绩效（年化/波动/夏普/Calmar/回撤/暴露/换手/逐年）、
组合层面、阈值鲁棒曲面。输出 `runs/value_timing_backtest.md/.json`。

**A. value 因子有效性（NW 显著性，t<−2 判有效）**

| 宽基 | PE(TTM) NW-t | PB NW-t | PB 多空 Q1−Q5 |
|---|---|---|---|
| 上证50 | −1.74（不显著） | **−2.78 ✓** | +32.9% |
| 沪深300 | −1.57（不显著） | **−2.23 ✓** | +26.6% |
| 中证500 | 0.04（无效） | **−2.55 ✓** | +28.1% |

关键修正：**PB（尤其 PB 中位数，t 达 −3.2）显著强于 PE(TTM)**；上一版用 PE-TTM
判「中证500 无效」是选错指标——PB 下中证500 同样显著有效。

**B. 择时绩效（PB 主指标，月频再平衡，无前视）**

| 宽基 | 规则 | 年化 | 夏普 | Calmar | 回撤 | 超额 |
|---|---|---|---|---|---|---|
| 上证50 | PB 线性(1.5) | +4.82% | 0.37 | 0.17 | −27.9% | −1.28% |
| 上证50 | 买入持有 | +6.10% | 0.22 | 0.09 | −71.3% | 0 |
| 沪深300 | PB 两档(0.5) | +5.01% | 0.31 | 0.14 | −36.7% | −2.77% |
| 沪深300 | 买入持有 | +7.78% | 0.29 | 0.11 | −70.8% | 0 |
| 中证500 | PB 线性(1-pct) | +2.71% | 0.17 | 0.08 | −34.4% | −4.18% |
| 中证500 | 买入持有 | +6.89% | 0.23 | 0.10 | −69.3% | 0 |

**C. 结论（如实）**
- 收益端无法增收益（PB 全阈值曲面超额均 ≤0），但**大盘价值宽基（上证50/沪深300）
  的 PB 择时能显著提夏普/Calmar**：上证50 夏普 0.22→0.37、Calmar 0.09→0.17、
  回撤 −71%→−28%，收益仅牺牲 1.3 点。中证500 即使 PB 有效，择时夏普仍降
  （0.23→0.17），价值择时不适用于中小成长宽基。
- 逐年结构：熊市/震荡市跑赢（2008 −24% vs −67%、2015 +8% vs −6%、2022 +6.7%
  vs −19.5%），单边大牛市踏空（2006-07 空仓 0% vs 满仓 +120~160%）——估值分位
  在牛市冲高过快导致过早离场，是估值择时的本质代价。
- 分位窗口越长越稳健（PB 全历史多空 +73%，3y 转负），5y 合理默认；换手极低
  （年化单边 <1.5 次）；阈值曲面平滑无孤立格点 → 非单点过拟合。
- 与 §7.18 QDII 溢价套利并列：这是继 QDII 溢价后，value 因子首次在「风险调整后
  收益」维度通过检验的信号（但仅限大盘价值宽基，且不能做空/不能增收益）。

**踩坑**：① 上一版默认 PE-TTM 是次优指标，PB 才显著——选指标前先做 NW 显著性
检验，勿凭直觉默认 PE；② 重叠样本须 NW(lag=12) 修正，否则会把高度重叠的月频
收益当成独立样本高估显著性。

### 7.20 三层组合联合回测（逆波动底仓 × PB 估值门控 × QDII 溢价门控）

§7.13/§7.19/§7.18 分别独立验证了三个有效组件，`scripts/portfolio_combined.py`
做组合层联合回测回答「三层叠加后净效果到底改善多少」。设计：18 只异构池共同
样本（2020-08~2026-06，1409 交易日），逆波动月频底仓，**乘法门控**
`W = W_逆波动(shift1) × gate(腿)`（门控空缺为现金不归一），统一单边成本 0.15%。
PB 门控 = 沪深300 PB 5 年分位三档（<30% 全仓/30~70% 半仓/≥70% 空仓）作用于
7 只 A 股股票腿；QDII 门控 = 溢价 z 飙升回避实盘约束版（floor=1%、min_hold=5）
作用于 6 只 QDII 腿。输出 `runs/portfolio_combined.md/.json`。

| 档位 | 年化 | 夏普 | 最大回撤 | Calmar | 换手/年 | 平均暴露 |
|---|---|---|---|---|---|---|
| A. 逆波动基线 | +5.93% | 0.84 | −10.0% | 0.59 | 1.44 | 97.0% |
| B. +PB 门控 | +5.79% | 0.99 | −7.1% | 0.81 | 1.67 | 89.4% |
| C. +QDII 门控 | +7.13% | 1.08 | −8.2% | 0.87 | 3.22 | 95.2% |
| D. 三层全开 | +6.98% | **1.28** | **−5.4%** | **1.30** | 3.45 | 87.6% |
| 等权(月度) | +6.62% | 0.51 | −17.6% | 0.38 | 0.18 | 97.0% |

**结论**：① 三层全开夏普 0.84→1.28（+52%）、回撤近腰斩（−10%→−5.4%）、
年化还 +1.0pp（已扣成本），三组件叠加无内耗、风险端协同；② 消融拆解与独立
结论一致——PB 门控「不增收益只控回撤」（B 档年化微降）、QDII 门控是收益发动机
（C 档 +1.2pp 且降回撤）；③ 逐年看价值集中在 2022 熊市（基线 −6.3% → 全开
−0.2%），代价是 2020H2/2025 牛市踏空 ~2pp——「熊市少亏 + 牛市少赚」的尾部
保护形态；④ 敏感性稳健：PB 规则 × 范围 × 成本（0/0.15%/0.30%/0.50%）共 9 变体
夏普 1.05~1.43 全部 >1（binary 样本内最高但不据此调参）。

**换手结构（是否按月换手？不是，是混合频率）**：D 档年化换手 3.45 的口径是
`Σ_t Σ_i |ΔW_i,t| / 年`——**18 只资产权重变动的加总**，可 >1，
**不等于「全仓一年换手 3.45 次」**；摊到单只中位数仅 9.8pp/年。按层拆解：
逆波动底仓（月频）1.44 占 42%、PB 门控增量（月频）0.23 占 7%、QDII 门控增量
（日频）1.78 占 52%。按发生日：月频再平衡执行日仅 68 天却贡献 40%，日频门控
触发 239 天贡献 60%；**78% 的交易日完全不动**。单次调仓规模很小——月频再平衡
平均只动 11.8% 组合资金、单次 QDII 门控动作仅 4.0%。年化换手成本 0.52%
（0.15% 单边），抬到 0.50% 单边夏普仍有 1.05，**换手不构成落地障碍**。
⚠️ 注意：月频再平衡的**执行日**是月末的**次一交易日**（`build_weights` 月末
赋值 → ffill → `shift(1)`），统计换手发生日时按「月末」归类会漏判。

**踩坑**：复用 `qdii_backtest.load_premium_history` 时缓存键是**裸代码**（513100），
池代码带 `.SH` 后缀须 `split(".")[0]` 剥离，否则缓存未命中走网络重拖。另：
算「翻转次数/年」时**年数必须按序列自身频率推断**——月频门控序列（如 PB 的
256 个月）若除以日频年数 5.59 会把 2.16 次/年误算成 8.2 次/年（4 倍虚高）。

**口径边界**：QDII 净值 T+1~T+2 公布的 ~1 日信息滞后（沿用 §7.18 全部回测同款
口径）；PB 用沪深300 分位代理全 A 股估值；样本 6 年不含 2008/2015 完整熊市。

### 7.21 三层组合实盘化（§9 待办第 3 项收口）

新增 `scripts/portfolio_live.py` 并接入 `qdii_daily.py` 第 3 步（crontab 工作日
15:30 自动跑，排在监控快照与溢价回测刷新之后——依赖后者刚刷新的溢价缓存）。
每个交易日收盘后输出「**明日目标持仓** + 三层门控状态 + 动作清单」到
`runs/portfolio_live.md / .json`。

**三层口径逐字复现回测（关键决策）**：
- 逆波动底仓：明日权重 = `build_weights(panel, "inverse_vol").iloc[-1]`——今日
  恰为本月最后交易日则为新算权重（明日执行再平衡），否则为上月末权重延续，
  与回测「月末赋值→ffill→shift(1)」语义等价；
- PB 门控：保持回测原样 shift(1) 语义。**探针实测确认**：该口径下 t+1 月实际
  使用的是 **t−1 月末**分位（比直觉多滞后一个月），但回测夏普 1.28 就是这个
  口径跑出来的，实盘不擅自「修正」——修正口径等于换了一个未回测的策略；
- QDII 门控：溢价序列末尾**追加一行 NaN 再跑状态机取末值**——状态机处理完
  最后一行真实数据后的 state 即 T+1 持仓（`spike_avoid_hold` 的 hold[i] 由
  i−1 轮 state 决定，末行天然是「今日持仓」，NaN 行是「明日持仓」）。

**数据刷新（免费通道，全部降级容忍）**：18 只 ETF 收盘价落后于今日时经
vibe broker（tencent 前复权链）增量拉取（start=最后日期−20 天，重叠区**新数据
优先**——qfq 以最新价为基准，新拉的重叠段含最新分红复权更准；实测把停在
2026-06-30 的数据一次刷到当日）；乐咕 PB 缓存未覆盖本月时重拉（schema 与
`value_timing_backtest.load_metrics` 互通）。任一步失败保留旧数据并告警，
ETF panel 末尾距今 >3 天则放弃月末再平衡宣告（防数据滞后误判）。

**动作清单**：与上一快照目标权重对比，≥0.5pp 才进清单；归因优先级
QDII 门控 > PB 调档 > 月度再平衡 > 波动率漂移。诚实边界：输出是「信号级」
目标权重（与回测换手口径一致），未接券商账户，实际执行须对照真实持仓。

**2026-08-31 首跑信号（也是当前实盘结论）**：今日为 8 月最后交易日 → 明日
（09-01）执行月度再平衡；沪深300 PB 分位 **73%** → A 股 7 腿全部**空仓**
（现金 10.1%）；QDII 6 腿全部持有无翻转——纳指溢价 11.21% 但 z=−0.13（高位
平稳非飙升），正是 §7.18 E「结构性高溢价期绝对阈值失效、相对变化信号才可用」
结论的实时印证。目标持仓：国债 68.8%、银华日利 5.4%、QDII 6 腿合计 10.7%、
黄金/豆粕/能源化工 4.9%、现金 10.1%。

**踩坑**：① vibe broker 必须 cwd=$HOME 子进程跑（src 包冲突，AGENTS.md §96）；
② 溢价净值 15:30 跑时一般只到 T−1~T−2（东财晚间才更新当日净值，21:53 重跑
已可拉到当日）——门控状态机天然按「最新可得数据」推进，与回测同款滞后，
不算 bug；③ 首跑无对比基准时动作清单为空，目标权重即建仓基准。

### 7.22 QDII 门控降频实验（日频 vs 周频，假设被推翻，维持日频）

§7.20 §5 发现 QDII 门控贡献组合 60% 换手、单次仅动 ~4% 资金，提出「降频到
周频可再压一半换手而几乎不损超额」的假设。`scripts/qdii_gate_weekly_test.py`
验证（报告 `runs/qdii_gate_weekly.md`）：周频口径 = 每周首个交易日取日频门控
值（该值已由上周五信息决定，无靠前）整周 ffill，即「周五决策、周一执行、
周内不动」。

**结果（D 档三层全开，样本刷到 2026-08-31 共 1453 日，0.15% 单边）**：

| 口径 | 年化 | 夏普 | 回撤 | 换手/年 | 换手成本/年 |
|---|---|---|---|---|---|
| 日频（现行） | +7.08% | 1.31 | −5.4% | 3.39 | 0.51% |
| 周频 | +5.89% | 1.08 | −6.4% | 3.27 | 0.49% |

C 档（无 PB 门控）交叉验证同向：夏普 1.08→0.89。成本抬到 0.5% 单边日频夏普
仍有 1.08 vs 周频 0.86——**任何成本假设下周频都不占优**。

**假设被推翻的机制**：① min_hold=5 本身已把翻转压到 2.6~6.6 回合/年，周频化
只能再压 3~12%（换手 3.39→3.27，仅 −3.5%），省成本 ~0.02pp/年，量级可忽略；
② 代价是「延迟回避暴露」——每腿 27~64 天（6 周~3 个月累计）处于「日频已空仓
避险、周频因周内锁定仍持有」状态，年化收益 −1.19pp。溢价飙升回避的收益恰恰
来自**飙升头几天的及时离场**，不是原以为的「躲开持续数周的高溢价段」。

**结论**：QDII 门控**维持日频**（portfolio_live.py 现行口径不改）。§7.20 §5 的
「60% 换手」是结构性特征而非可压缩浪费——它单次动 4% 资金、年化成本 0.51%，
换来 +1.2pp 收益，是组合里性价比最高的换手。

**口径备注**：本次 D-daily（+7.08%/夏普 1.31）略高于 §7.20 原值（+6.98%/1.28），
因实盘化已把 ETF 数据从 2026-06-30 刷到 2026-08-31，样本多 44 个交易日。

### 7.23 蛋卷 VIP 估值历史面板（§9 待办第 1 项解锁：7 年 65 指数日频面板）

**背景**：§9 待办第 1 项「基本面全历史截面 IC」此前卡在拿不到多指数的估值
历史面板——乐咕只有沪深300/上证50/中证500 三只宽基。本节解锁了蛋卷的
**VIP 估值表历史面板**：1707 期 × 65 指数，2019-09-04 ~ 2026-08-31。

**Cookie 获取**（踩坑）：Chrome DevTools 的 **「Save all as HAR」默认剥离
Cookie/Set-Cookie**（HAR 里 `cookies` 数组全空、`Cookie` 头 0 条，但
`/djapi/account` 响应能看出已登录 uid）。必须用
**「Save all as HAR (with sensitive data)」**重导出，或直接从 Network 面板
某条请求的 Headers 里复制 `cookie:` 整行。Cookie 存 `data/.danjuan_cookie`
（data/ 已在 .gitignore，不会入库）。

**接口探索路径（失败的都记下来）**：
| 接口 | 结果 |
|---|---|
| `/djapi/index_eva/dj`（免登录） | ✅ 63 指数**单截面**快照，项目已在用（§7.17） |
| `/djapi/index_eva/detail/{code}` | 单指数**当前**快照（仅多一个 peg），无历史 |
| `/djapi/index_eva/dj/history` | 未登录返回「请重新登录」，登录后恒 **500 服务器错误**（参数不明，放弃） |
| **`/djapi/fundx/base/vip/valuation/show/detail?id=N`** | ✅ **第 N 期整张估值表**（62 指数 × pe/pb/roe/股息率/盈利收益率 + 5y/10y 分位 + 场内/场外基金代码） |

接口路径是从 Next.js 的 `_buildManifest.js`（buildId 从首页 HTML 取）拿到
93 条路由 → `/n/screw/valuation-table` 页 → 其 chunk 里的调用点挖出来的。

**关键坑：`source` 与 `id` 互斥**。带 `source=lsd` 时 `id` 被忽略、恒返回最新期；
只有**单独传 `id`** 才能取历史期。所以遍历历史只能 `?id=N` 逐个打（10444 个 id
8 并发约 8.5 分钟，其中 1/3 是空洞/他表记录）。

**两张表的拆分**（抓取只能用 id 遍历，id 空间里混了两张不同的估值表）：
用「与最新期指数集合的 **Jaccard 相似度**」判别，实测在 **0.3 处有明显断层**
（0.17 与 0.41 之间无样本），无歧义：

| 表 | 期数 | 时间跨度 | 指数数 |
|---|---|---|---|
| **lsd（螺丝钉，主力）** | 1707 | 2019-09-04 ~ 2026-08-31 | 28 → 62 |
| other（另一张，已停更） | 1733 | 2019-08-20 ~ 2024-12-13 | 10 → 15 |

**可信度核验（与乐咕 PB 交叉验证，分年段）**：

| 指数 | 2019-2023 相关 / 偏离 | 2024+ 相关 / 偏离 |
|---|---|---|
| 中证500 | 0.947 / −3.3% | 0.968 / +1.5% |
| 沪深300 | 0.934 / −7.4% | **0.970 / +22.7%** |
| 上证50 | 0.770 / −5.3% | **0.942 / +25.0%** |

→ 段内相关 0.93~0.97 说明**序列本身可信**；但沪深300/上证50 在 **2024 年发生
约 +30% 的水平跳变**（蛋卷侧换口径/换数据源，非随机错误），中证500 无跳变。
**影响**：截面 IC 用横截面排序（分位数），对统一口径的水平偏差不敏感，
**不影响主力用途**；但蛋卷 PB 的绝对水平若要与乐咕时序（§7.19）混用，
须先分段对齐，否则误判估值高低。

**边界**：① **幸存者偏差**——指数池从 28 扩到 62、面板共 144 个 index_code，
历史被移出者后期无数据；只能按「当期存在的指数」算截面（天然规避），
不能回测「全历史持有某指数」；② **PE=0 = 不适用**（银行/证券等金融行业），
对应分位也是 0，做 IC 前须剔除或改用 PB；③ **QDII 指数（NDX/SP500/HK\*）的
ETF 存在显著溢价**（§7.18），用 ETF 二级价格算收益会混入溢价波动，
应单独分组或剔除；④ **会员数据**，仅供个人研究勿分发，cookie 会过期
（增量更新需重新导出）；⑤ 最新期 62 指数中仅 **51 个有场内 ETF 代码**，
其余只能走场外或不可交易。

**产物**：`scripts/danjuan_panel_fetch.py`（抓取，断点续传 + 8 并发）、
`scripts/danjuan_panel_split.py`（两表拆分 + 交叉验证）、
`data/fundamental/danjuan_valuation_lsd.csv`（干净面板 63549 行）、
`runs/danjuan_panel_report.md`（体检报告）。

### 7.3 数据正确性核验

引擎实际使用的数据（`runs/<name>/artifacts/ohlcv_*.csv`）与注入的 tencent
前复权 CSV **逐字节一致**（如 512480 于 2024-06-06：
`0.358,0.360,0.351,0.352,21251410.0`）。这一项必须每次首跑都核——§6.6 的坑
会让引擎悄悄改用不复权数据，且回测**不会报错**，只是结果全错。

## 8. 边界与已知限制

- 回测评估终点必须**早于今天**（vibe 缓存只对已完结日期区间生效）
- **vibe 0.1.14 无 `evaluation_start_date`**：评估窗口只能靠 signal_engine 的
  `EVAL_START` 门槛实现；vibe 自家指标仍是全区间口径，看
  `evaluation_metrics.json` 才是评估窗口的真实表现
- **回测日期轴由 data_map 决定**，不是 config 的 `start_date`——注入的缓存帧
  有多长，回测就从哪天开始（这也是 warmup 能生效的原因，见 §4.1）
- 数据源不可混用：tencent 前复权 vs akshare 不复权，同一标的同日收盘价可差
  2 倍以上。全链路固定走 tencent，akshare 仅作缓存投递通道
- vibetrading 的 `~/source/Vibe-Trading` 源码版本**新于**已安装的 0.1.14，
  查文档/源码时以 site-packages 里的实际文件为准（行号都对不上）
- QDII 影子 IOPV 已实装真实 IOPV/折价率 + 美股/港股/德国DAX/日经影子调整 +
  汇率修正（§7.14 + §7.18 精细版），但仍是一阶近似：未计底层成分权重漂移、
  汇率用中行日频牌价（非日内分时）、周末牌价不变时 fx_chg≈0
- `report_sentiment` 为空时融合信号自动退化为纯 RSRS
- Streamlit Dashboard（Design P4）未动，`streamlit` 包未安装

## 9. 策略有效性待办

链路、滑动窗口对照、参数扫描、显著性检验、扩展池验证、降频验证、因子诊断、
新因子批量筛选、异构池检验、时序择时 + 大类资产轮动、**基本面/估值快照**、
**基本面时序估值择时（§7.16）**、**value 时序择时完整回测（§7.19）**、
**蛋卷 63 指数截面估值排名（§7.17）**、**QDII 溢价实装（§7.14）**、
**QDII 溢价套利回测 + 产品化（§7.18）**、**风险平价兜底**、**三层组合联合回测
（§7.20）**、**三层组合实盘化（§7.21）**均已完成（§7.4~§7.21）。**最终结论
（收口）**：价量数据在 ETF 宇宙上的三条主动 alpha 路线——截面因子（16 个 +
RSRS）、时序趋势择时（多/空现金）、大类资产轮动（切债/金/动量 top1）——
**全部证伪**（天花板≈0）。落地成果：

1. **基本面/估值因子**：快照诊断（§7.13）+ 时序估值择时检验（§7.16）+ 完整回测
   （§7.19）已完成。PB 因子在 3 宽基上 NW 显著（t<−2，多空 +17%~+33%），强于
   PE(TTM)；PB 择时对大盘价值宽基（上证50/沪深300）可显著提夏普/Calmar（上证50
   夏普 0.22→0.37、回撤 −71%→−28%），中证500 不适用。截面 IC 仍受限于「多年 ×
   多标的」估值数据缺口（Tushare 需 2000 积分）。
2. **QDII 溢价套利**：真实 IOPV/折价率 + 影子调整（§7.14 含 DAX/日经 + 汇率）
   + 历史溢价回测 + 每日定时任务已全部落地（§7.18）。结论：溢价>3% 是「回避
   买入/减仓」告警（超额来自恐慌期溢价回归，不能做空）；折价<−3% 是更可信的
   低波动逢低买入窗口（纳指年化约 9%、回撤仅 −1.8%）。是当前唯一站得住的独立
   alpha，已产品化为收盘后自动跑监控 + 回测。
   **⚠️ 2026 专项补记（§7.18 E）+ 相对变化告警落地（§7.18 F/G）**：高溢价已从脉冲式
   恐慌转为额度告罄的结构性常态（纳指溢价均值 7.05%、全年 94% 时间 >3%），3% 绝对
   阈值失效、折价窗口全年零触发。已将监控从「绝对水平告警」升级为「相对变化告警」
   （溢价一阶差分 z 分数），全量+按年回测证明「飙升回避」是可用尾部对冲（纳指夏普
   0.89→1.95、暴露 ~95%）、「回落买入」是逢低加仓参考。**实盘约束版（§7.18 G）**
   补上 floor/min_hold/成本三大约束后，真实净超额「纳指被高估、其余基本实」——组合
   平均净年化超额 +8.4pp（纳指 +8.97、标普 +15.32、德国 +15.94），仍可作减仓/对冲
   信号，但须容忍限购「想买回时买不回」的执行风险。
3. **风险平价兜底**：逆波动组合夏普 0.87（18 池）/ 1.53（核心三腿），是
   AlphaQuant 的兜底输出（§7.15）。
4. **三层组合联合回测（§7.20）**：逆波动底仓 × PB 估值门控 × QDII 溢价门控，
   三层全开夏普 0.84→1.28、回撤 −10%→−5.4%、年化 +1.0pp（已扣成本）——
   **这是 AlphaQuant 的正式组合输出形态**：兜底底仓 + 两个独立信号门控。

剩余方向按优先级：

1. **基本面全历史截面 IC（数据缺口已解锁 §7.23，剩余算 IC）**：Tushare 路线仍
   卡积分（40203），但**已用蛋卷 VIP 接口绕开**——拿到 **1707 期 × 65 指数、
   2019-09 ~ 2026-08 的日频估值面板**（`data/fundamental/danjuan_valuation_lsd.csv`，
   含 pe/pb/roe/股息率/盈利收益率 + 5y/10y 分位），远超原计划的 Tushare 方案。
   下一步：把 51 个有场内 ETF 代码的指数映射到可交易标的 → 拉收益序列
   （tencent 前复权通道已有）→ 算「估值分位 → 未来 N 日收益」的截面 IC + 分层
   收益。注意 §7.23 记录的三条边界（幸存者偏差、PE=0 剔除、QDII 溢价污染）。
2. **QDII 套利后续增强**（产品化已落地，此为可选优化）：盘中实时轮询（当前为
   收盘快照）、折价买入信号接入实盘提醒、汇率日内分时数据（交易成本/换手已纳入
   §7.18 G 实盘约束版回测）
3. **组合实盘化（已完成，§7.21）**：`scripts/portfolio_live.py` 接入
   `qdii_daily.py` 第 3 步，crontab 每个交易日收盘后自动输出「明日目标持仓 +
   三层门控状态 + 动作清单」（月频再平衡 + 每日 QDII 门控刷新，口径与 §7.20
   回测逐字一致）。后续可选增强：接入券商账户实际持仓对账、动作推送提醒。

方法论沉淀（跨轮累计，与 §7.12 前视偏差并列）：
- 任何回测信号用 t−1 及之前信息决定 t 日持仓并逐行核对 shift（§7.12）
- scipy 数值优化前先归一化目标函数量级，否则数值梯度失效返回初始点（§7.15）
- 接第三方接口先核对符号约定（东财折价率=折价、正值非溢价）与代码格式（§7.14）
- pandas `DataFrame(dict, index=...)` 会按索引对齐 dict 内 Series，RangeIndex 与
  日期索引对齐失败 → 全 NaN，须 `.to_numpy()` 传裸值（§7.16）
- 免费数据源的「时间跨度/标的覆盖」要代码实测核实，勿信二手注释（§7.16 把乐咕
  「月频完整历史」误记成「近 1 年」，把全历史分位误标「1y 分位」）
- 有份额拆分的 ETF/基金回测，收益必须用「累计净值/单位净值」复权因子折算前复权价
  且方向是 `close × (factor / f_last)`，搞反或漏做都会让收益全错（§7.18 纳指被
  算成 0.06% 实际 21.49%）
- 阈值量纲要一致：溢价用小数（0.03=3%）时阈值也要 `/100`，否则 3.0 被当 300%
  恒满仓策略空转（§7.18）

方法论沉淀：任何回测信号必须用 t-1 及之前信息决定 t 日持仓并逐行核对 shift，
否则「全区间亮眼」必是前视假象（§7.12 轮动策略的教训）。
